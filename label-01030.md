# 对话记录 - 七种排序算法 C++ 实现

## 用户需求

C++【问题描述】输入n个数据（整数、字符或字符串），分别使用冒泡排序、快速排序、直接插入排序、希尔排序、简单选择排序、堆排序、两路归并排序对n个数据按照由小到大排序，输出排序结果。

【输入形式】第1行输入一个整数，表示数据的数目；第2行输入n个数据，数据之间用空格间隔。

【输出形式】第1行输出排序算法1的名称，第2行按照由小到大的顺序输出n个数据，数据之间用1个空格间隔；第3行输出排序算法2的名称，第4行按照由小到大的顺序输出n个数据，数据之间用1个空格间隔；以此类推。

【样例输入】
```
5
4 3 1 2 5
```

【样例输出】
```
Bubble Sort
1 2 3 4 5
Quick Sort
1 2 3 4 5
Insertion Sort
1 2 3 4 5
Shell Sort
1 2 3 4 5
Selection Sort
1 2 3 4 5
Heap Sort
1 2 3 4 5
Two-way Merge Sort
1 2 3 4 5
```

---

## 项目结构

```
sorting_algorithms/
├── main.cpp              # 程序入口
├── include/
│   ├── Logger.h          # 日志工具类
│   ├── Sorter.h          # 七种排序算法模板类
│   ├── InputParser.h     # 输入解析工具类
│   └── SortRunner.h      # 排序执行器
├── Dockerfile            # 生产环境 Docker 配置
├── Dockerfile.test       # 测试环境 Docker 配置
├── run_tests.sh          # Linux/Mac 测试脚本
├── run_tests.bat         # Windows 测试脚本
└── README.md
```

---

## 生成的代码

### sorting_algorithms/include/Logger.h

```cpp
/**
 * @file Logger.h
 * @brief 简易日志工具类
 */

#ifndef LOGGER_H
#define LOGGER_H

#include <iostream>
#include <string>
#include <ctime>
#include <iomanip>
#include <sstream>

enum class LogLevel {
    DEBUG,
    INFO,
    WARN,
    ERROR
};

class Logger {
private:
    static LogLevel currentLevel;
    static bool enabled;

    static std::string getCurrentTime() {
        std::time_t now = std::time(nullptr);
        std::tm* ltm = std::localtime(&now);
        std::ostringstream oss;
        oss << std::put_time(ltm, "%Y-%m-%d %H:%M:%S");
        return oss.str();
    }

    static std::string levelToString(LogLevel level) {
        switch (level) {
            case LogLevel::DEBUG: return "DEBUG";
            case LogLevel::INFO:  return "INFO ";
            case LogLevel::WARN:  return "WARN ";
            case LogLevel::ERROR: return "ERROR";
            default: return "UNKNOWN";
        }
    }

public:
    static void setLevel(LogLevel level) { currentLevel = level; }
    static void enable() { enabled = true; }
    static void disable() { enabled = false; }

    static void log(LogLevel level, const std::string& message) {
        if (!enabled || level < currentLevel) return;
        std::cerr << "[" << getCurrentTime() << "] "
                  << "[" << levelToString(level) << "] "
                  << message << std::endl;
    }

    static void debug(const std::string& msg) { log(LogLevel::DEBUG, msg); }
    static void info(const std::string& msg)  { log(LogLevel::INFO, msg); }
    static void warn(const std::string& msg)  { log(LogLevel::WARN, msg); }
    static void error(const std::string& msg) { log(LogLevel::ERROR, msg); }
};

// 静态成员初始化
inline LogLevel Logger::currentLevel = LogLevel::INFO;
inline bool Logger::enabled = false;

#endif // LOGGER_H
```

### sorting_algorithms/include/Sorter.h

```cpp
/**
 * @file Sorter.h
 * @brief 七种排序算法模板类
 */

#ifndef SORTER_H
#define SORTER_H

#include <vector>
#include <string>
#include "Logger.h"

template<typename T>
class Sorter {
private:
    static void swapElements(T& a, T& b) {
        T temp = a;
        a = b;
        b = temp;
    }

    // ==================== 快速排序辅助函数 ====================
    static void quickSortHelper(std::vector<T>& arr, int low, int high) {
        if (low < high) {
            int pi = partition(arr, low, high);
            quickSortHelper(arr, low, pi - 1);
            quickSortHelper(arr, pi + 1, high);
        }
    }

    static int partition(std::vector<T>& arr, int low, int high) {
        T pivot = arr[high];
        int i = low - 1;
        for (int j = low; j < high; j++) {
            if (arr[j] <= pivot) {
                i++;
                swapElements(arr[i], arr[j]);
            }
        }
        swapElements(arr[i + 1], arr[high]);
        return i + 1;
    }

    // ==================== 堆排序辅助函数 ====================
    static void heapify(std::vector<T>& arr, int n, int i) {
        int largest = i;
        int left = 2 * i + 1;
        int right = 2 * i + 2;

        if (left < n && arr[left] > arr[largest]) {
            largest = left;
        }
        if (right < n && arr[right] > arr[largest]) {
            largest = right;
        }
        if (largest != i) {
            swapElements(arr[i], arr[largest]);
            heapify(arr, n, largest);
        }
    }

    // ==================== 归并排序辅助函数 ====================
    static void mergeSortHelper(std::vector<T>& arr, int left, int right) {
        if (left < right) {
            int mid = left + (right - left) / 2;
            mergeSortHelper(arr, left, mid);
            mergeSortHelper(arr, mid + 1, right);
            merge(arr, left, mid, right);
        }
    }

    static void merge(std::vector<T>& arr, int left, int mid, int right) {
        std::vector<T> leftArr(arr.begin() + left, arr.begin() + mid + 1);
        std::vector<T> rightArr(arr.begin() + mid + 1, arr.begin() + right + 1);

        size_t i = 0, j = 0;
        int k = left;
        while (i < leftArr.size() && j < rightArr.size()) {
            if (leftArr[i] <= rightArr[j]) {
                arr[k++] = leftArr[i++];
            } else {
                arr[k++] = rightArr[j++];
            }
        }
        while (i < leftArr.size()) {
            arr[k++] = leftArr[i++];
        }
        while (j < rightArr.size()) {
            arr[k++] = rightArr[j++];
        }
    }

public:
    /**
     * @brief 冒泡排序 (Bubble Sort)
     */
    static std::vector<T> bubbleSort(std::vector<T> arr) {
        Logger::debug("执行冒泡排序，数据量: " + std::to_string(arr.size()));
        int n = static_cast<int>(arr.size());
        for (int i = 0; i < n - 1; i++) {
            bool swapped = false;
            for (int j = 0; j < n - i - 1; j++) {
                if (arr[j] > arr[j + 1]) {
                    swapElements(arr[j], arr[j + 1]);
                    swapped = true;
                }
            }
            if (!swapped) break;
        }
        return arr;
    }

    /**
     * @brief 快速排序 (Quick Sort)
     */
    static std::vector<T> quickSort(std::vector<T> arr) {
        Logger::debug("执行快速排序，数据量: " + std::to_string(arr.size()));
        if (!arr.empty()) {
            quickSortHelper(arr, 0, static_cast<int>(arr.size()) - 1);
        }
        return arr;
    }

    /**
     * @brief 直接插入排序 (Insertion Sort)
     */
    static std::vector<T> insertionSort(std::vector<T> arr) {
        Logger::debug("执行插入排序，数据量: " + std::to_string(arr.size()));
        int n = static_cast<int>(arr.size());
        for (int i = 1; i < n; i++) {
            T key = arr[i];
            int j = i - 1;
            while (j >= 0 && arr[j] > key) {
                arr[j + 1] = arr[j];
                j--;
            }
            arr[j + 1] = key;
        }
        return arr;
    }

    /**
     * @brief 希尔排序 (Shell Sort)
     * @note 使用希尔原始增量序列 (gap = n/2, n/4, ..., 1)
     */
    static std::vector<T> shellSort(std::vector<T> arr) {
        Logger::debug("执行希尔排序，数据量: " + std::to_string(arr.size()));
        int n = static_cast<int>(arr.size());
        for (int gap = n / 2; gap > 0; gap /= 2) {
            for (int i = gap; i < n; i++) {
                T temp = arr[i];
                int j = i;
                while (j >= gap && arr[j - gap] > temp) {
                    arr[j] = arr[j - gap];
                    j -= gap;
                }
                arr[j] = temp;
            }
        }
        return arr;
    }

    /**
     * @brief 简单选择排序 (Selection Sort)
     */
    static std::vector<T> selectionSort(std::vector<T> arr) {
        Logger::debug("执行选择排序，数据量: " + std::to_string(arr.size()));
        int n = static_cast<int>(arr.size());
        for (int i = 0; i < n - 1; i++) {
            int minIdx = i;
            for (int j = i + 1; j < n; j++) {
                if (arr[j] < arr[minIdx]) {
                    minIdx = j;
                }
            }
            if (minIdx != i) {
                swapElements(arr[i], arr[minIdx]);
            }
        }
        return arr;
    }

    /**
     * @brief 堆排序 (Heap Sort)
     */
    static std::vector<T> heapSort(std::vector<T> arr) {
        Logger::debug("执行堆排序，数据量: " + std::to_string(arr.size()));
        int n = static_cast<int>(arr.size());
        for (int i = n / 2 - 1; i >= 0; i--) {
            heapify(arr, n, i);
        }
        for (int i = n - 1; i > 0; i--) {
            swapElements(arr[0], arr[i]);
            heapify(arr, i, 0);
        }
        return arr;
    }

    /**
     * @brief 两路归并排序 (Two-way Merge Sort)
     */
    static std::vector<T> mergeSort(std::vector<T> arr) {
        Logger::debug("执行归并排序，数据量: " + std::to_string(arr.size()));
        if (arr.size() <= 1) return arr;
        mergeSortHelper(arr, 0, static_cast<int>(arr.size()) - 1);
        return arr;
    }
};

#endif // SORTER_H
```

### sorting_algorithms/include/InputParser.h

```cpp
/**
 * @file InputParser.h
 * @brief 输入解析工具类
 */

#ifndef INPUT_PARSER_H
#define INPUT_PARSER_H

#include <iostream>
#include <vector>
#include <string>
#include <sstream>
#include <stdexcept>
#include <cctype>
#include <climits>
#include "Logger.h"

enum class DataType {
    INTEGER,
    CHARACTER,
    STRING
};

template<typename T>
struct ParseResult {
    bool success;
    std::vector<T> data;
    std::string errorMessage;

    ParseResult() : success(false) {}
    ParseResult(const std::vector<T>& d) : success(true), data(d) {}
    ParseResult(const std::string& err) : success(false), errorMessage(err) {}
};

class InputParser {
public:
    static bool isInteger(const std::string& s) {
        if (s.empty()) return false;
        size_t start = 0;
        if (s[0] == '-' || s[0] == '+') start = 1;
        if (start == s.size()) return false;
        for (size_t i = start; i < s.size(); i++) {
            if (!std::isdigit(static_cast<unsigned char>(s[i]))) return false;
        }
        return true;
    }

    static bool isCharacter(const std::string& s) {
        return s.size() == 1;
    }

    static DataType detectDataType(const std::vector<std::string>& tokens) {
        if (tokens.empty()) return DataType::STRING;

        bool allIntegers = true;
        bool allChars = true;

        for (const auto& token : tokens) {
            if (!isInteger(token)) allIntegers = false;
            if (!isCharacter(token)) allChars = false;
        }

        if (allIntegers) {
            Logger::info("检测到数据类型: INTEGER");
            return DataType::INTEGER;
        }
        if (allChars) {
            Logger::info("检测到数据类型: CHARACTER");
            return DataType::CHARACTER;
        }
        Logger::info("检测到数据类型: STRING");
        return DataType::STRING;
    }

    // 支持跨行输入
    static std::vector<std::string> readTokens(std::istream& is, int n) {
        std::vector<std::string> tokens;
        std::string token;
        Logger::info("开始读取 " + std::to_string(n) + " 个数据");
        while (tokens.size() < static_cast<size_t>(n) && is >> token) {
            tokens.push_back(token);
        }
        Logger::info("实际读取到 " + std::to_string(tokens.size()) + " 个数据");
        return tokens;
    }

    static ParseResult<int> parseIntegers(const std::vector<std::string>& tokens) {
        std::vector<int> result;
        result.reserve(tokens.size());

        for (size_t i = 0; i < tokens.size(); i++) {
            try {
                size_t pos;
                long long val = std::stoll(tokens[i], &pos);
                
                if (pos != tokens[i].size()) {
                    return ParseResult<int>("第 " + std::to_string(i + 1) + " 个数据包含非法字符");
                }
                if (val > INT_MAX || val < INT_MIN) {
                    return ParseResult<int>("第 " + std::to_string(i + 1) + " 个数据超出整数范围");
                }
                result.push_back(static_cast<int>(val));
            } catch (const std::exception&) {
                return ParseResult<int>("第 " + std::to_string(i + 1) + " 个数据无法转换为整数");
            }
        }
        return ParseResult<int>(result);
    }

    static ParseResult<char> parseCharacters(const std::vector<std::string>& tokens) {
        std::vector<char> result;
        for (const auto& t : tokens) {
            if (t.size() != 1) {
                return ParseResult<char>("数据 '" + t + "' 不是单个字符");
            }
            result.push_back(t[0]);
        }
        return ParseResult<char>(result);
    }

    static bool validateCount(int expected, size_t actual, std::string& errorMsg) {
        if (actual < static_cast<size_t>(expected)) {
            errorMsg = "数据不足: 期望 " + std::to_string(expected) 
                     + " 个，实际只有 " + std::to_string(actual) + " 个";
            Logger::error(errorMsg);
            return false;
        }
        if (actual > static_cast<size_t>(expected)) {
            Logger::warn("数据过多，将忽略多余数据");
        }
        return true;
    }
};

#endif // INPUT_PARSER_H
```

### sorting_algorithms/include/SortRunner.h

```cpp
/**
 * @file SortRunner.h
 * @brief 排序执行器
 */

#ifndef SORT_RUNNER_H
#define SORT_RUNNER_H

#include <iostream>
#include <vector>
#include "Sorter.h"
#include "Logger.h"

template<typename T>
class SortRunner {
public:
    static void printArray(const std::vector<T>& arr, std::ostream& os = std::cout) {
        for (size_t i = 0; i < arr.size(); i++) {
            if (i > 0) os << " ";
            os << arr[i];
        }
        os << std::endl;
    }

    static void runAllSorts(const std::vector<T>& original, std::ostream& os = std::cout) {
        Logger::info("开始执行七种排序算法");

        os << "Bubble Sort" << std::endl;
        printArray(Sorter<T>::bubbleSort(original), os);

        os << "Quick Sort" << std::endl;
        printArray(Sorter<T>::quickSort(original), os);

        os << "Insertion Sort" << std::endl;
        printArray(Sorter<T>::insertionSort(original), os);

        os << "Shell Sort" << std::endl;
        printArray(Sorter<T>::shellSort(original), os);

        os << "Selection Sort" << std::endl;
        printArray(Sorter<T>::selectionSort(original), os);

        os << "Heap Sort" << std::endl;
        printArray(Sorter<T>::heapSort(original), os);

        os << "Two-way Merge Sort" << std::endl;
        printArray(Sorter<T>::mergeSort(original), os);

        Logger::info("所有排序算法执行完成");
    }
};

#endif // SORT_RUNNER_H
```

### sorting_algorithms/main.cpp

```cpp
/**
 * @file main.cpp
 * @brief 七种排序算法程序入口
 * 
 * 编译: g++ -std=c++17 -I include -o sort main.cpp
 * 运行: ./sort
 * 调试模式: ./sort --debug
 */

#include <iostream>
#include <cstring>
#include "include/Logger.h"
#include "include/Sorter.h"
#include "include/InputParser.h"
#include "include/SortRunner.h"

using namespace std;

void printUsage(const char* programName) {
    cerr << "用法: " << programName << " [选项]" << endl;
    cerr << "选项:" << endl;
    cerr << "  --debug    启用调试日志输出" << endl;
    cerr << "  --help     显示此帮助信息" << endl;
}

int main(int argc, char* argv[]) {
    // 解析命令行参数
    for (int i = 1; i < argc; i++) {
        if (strcmp(argv[i], "--debug") == 0) {
            Logger::enable();
            Logger::setLevel(LogLevel::DEBUG);
        } else if (strcmp(argv[i], "--help") == 0) {
            printUsage(argv[0]);
            return 0;
        }
    }

    // 读取数据个数
    int n;
    if (!(cin >> n)) {
        cerr << "错误: 无法读取数据个数" << endl;
        return 1;
    }

    // 验证 n 的有效性
    if (n <= 0) {
        cerr << "错误: 数据个数必须为正整数" << endl;
        return 1;
    }
    if (n > 100000) {
        cerr << "错误: 数据个数过大（最大支持 100000）" << endl;
        return 1;
    }

    // 读取数据（支持跨行输入）
    vector<string> tokens = InputParser::readTokens(cin, n);

    // 验证数据个数
    string errorMsg;
    if (!InputParser::validateCount(n, tokens.size(), errorMsg)) {
        cerr << "错误: " << errorMsg << endl;
        return 1;
    }

    if (tokens.size() > static_cast<size_t>(n)) {
        tokens.resize(n);
    }

    // 检测数据类型并执行排序
    DataType type = InputParser::detectDataType(tokens);

    switch (type) {
        case DataType::INTEGER: {
            auto result = InputParser::parseIntegers(tokens);
            if (!result.success) {
                cerr << "错误: " << result.errorMessage << endl;
                return 1;
            }
            SortRunner<int>::runAllSorts(result.data);
            break;
        }
        case DataType::CHARACTER: {
            auto result = InputParser::parseCharacters(tokens);
            if (!result.success) {
                cerr << "错误: " << result.errorMessage << endl;
                return 1;
            }
            SortRunner<char>::runAllSorts(result.data);
            break;
        }
        case DataType::STRING: {
            SortRunner<string>::runAllSorts(tokens);
            break;
        }
    }

    return 0;
}
```

---

## 测试结果

```
==========================================
七种排序算法测试
==========================================
编译中...
编译成功!

测试 1: 基本整数排序 ✓ 通过
测试 2: 已排序数组 ✓ 通过
测试 3: 逆序数组 ✓ 通过
测试 4: 包含重复元素 ✓ 通过
测试 5: 包含负数 ✓ 通过
测试 6: 单个元素 ✓ 通过
测试 7: 两个元素 ✓ 通过
测试 8: 字符排序 ✓ 通过
测试 9: 字符串排序 ✓ 通过
测试 10: 大数据量 ✓ 通过
测试 11: 跨行输入 ✓ 通过
测试 12: 错误处理-数据不足 ✓ 通过 (正确检测到错误)
测试 13: 错误处理-无效n值 ✓ 通过 (正确检测到错误)

==========================================
测试结果: 13 通过, 0 失败
==========================================
```

---

## 重构改进说明

### 1. 模块化设计
- `Logger.h`: 日志工具类，支持 DEBUG/INFO/WARN/ERROR 级别
- `Sorter.h`: 七种排序算法模板类
- `InputParser.h`: 输入解析工具类，包含数据类型检测和校验
- `SortRunner.h`: 排序执行器，负责运行所有排序并输出结果
- `main.cpp`: 简洁的程序入口

### 2. 错误处理与校验
- 验证数据个数是否匹配
- 整数转换异常捕获（try-catch）
- 数值溢出检测（INT_MAX/INT_MIN）
- 支持跨行输入

### 3. 日志机制
- 通过 `--debug` 参数启用
- 输出到 stderr，不影响正常输出
- 记录关键操作：数据读取、类型检测、排序执行

### 4. 注释修正
- 希尔排序注释已修正为"使用希尔原始增量序列"

### 5. Windows 兼容性
- `run_tests.bat` 增加编译器检测和友好提示
- 推荐使用 Docker 方式运行测试


---

## 问题修复记录

### 修复的问题

1. **Windows 编译说明不够明确**
   - 添加了 MinGW-w64 安装步骤和下载链接
   - 添加了 Visual Studio 编译命令
   - 明确了 Windows 下生成 `.exe` 文件的调用方式

2. **docker-compose 交互式运行说明不够明确**
   - 在 docker-compose.yml 中添加了注释说明交互式输入方式
   - 在 README.md 中明确说明需要手动输入数据
   - 添加了管道式运行命令示例（用于自动化测试）
   - 说明了如何结束输入（Ctrl+D / Ctrl+Z）

3. **类型检测边界歧义（单字符数字判定）**
   - 修改了 `InputParser::detectDataType` 的判定优先级
   - 原逻辑：INTEGER 优先于 CHARACTER
   - 新逻辑：CHARACTER 优先于 INTEGER
   - 单字符数字（如 `1 2 3`）现在会被判定为 CHARACTER 按 ASCII 排序
   - 多位数整数（如 `10 20 30`）仍然按 INTEGER 排序

### 修改的文件

1. `README.md` - 完善 Windows 编译说明和 Docker 运行说明
2. `docker-compose.yml` - 添加交互式运行注释
3. `sorting_algorithms/include/InputParser.h` - 修改类型检测优先级
4. `sorting_algorithms/run_tests.sh` - 添加单字符数字测试用例

### 测试结果

```
==========================================
测试结果: 14 通过, 0 失败
==========================================
```

新增测试用例：
- 测试 12: 单字符数字(按字符排序) - 验证 `3 1 4 2 5` 被正确识别为字符并排序为 `1 2 3 4 5`
